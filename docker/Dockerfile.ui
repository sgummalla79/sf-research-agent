# Stage 1 — build the Vue app
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# Stage 2 — serve with Caddy (no Node, no secrets)
FROM caddy:alpine
COPY --from=build /app/dist /usr/share/caddy
COPY docker/Caddyfile.ui /etc/caddy/Caddyfile
EXPOSE 80
