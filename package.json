{
  "name": "sf-research-agent",
  "private": true,
  "scripts": {
    "dev": "concurrently --names backend,frontend --prefix-colors blue.bold,green.bold --kill-others-on-fail \"cd backend && .venv/bin/uvicorn api.app:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "install:frontend": "cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
