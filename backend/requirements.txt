# Core orchestration
langgraph>=0.2.0

# LLM clients — active
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0       # used for Perplexity (OpenAI-compatible API)
langchain-core>=0.3.0

# LLM clients — Google (Gemini for deep architectural research)
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0

# Anthropic SDK (direct)
anthropic>=0.36.0

# OpenAI SDK — used by langchain-openai for Perplexity calls
openai>=1.40.0

# Database — PostgreSQL backend
psycopg[binary,pool]>=3.1.0
langgraph-checkpoint-postgres>=1.0.0

# Database — SQLite backend (zero-config, great for local dev + testing)
langgraph-checkpoint-sqlite>=1.0.0
aiosqlite>=0.19.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9      # required by FastAPI for UploadFile / file uploads
tenacity>=8.2.0              # LLM retry logic with exponential backoff

# File parsing (document upload support)
pypdf>=4.0.0
python-docx>=1.1.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
cryptography>=42.0.0   # Fernet symmetric encryption for API key storage

# Auth — Auth0 JWT verification + Management API calls
python-jose[cryptography]>=3.3.0
httpx>=0.27.0
