# v1.0.4 — 2026-05-14

## Features
- auto-deploy to cluster after build
- auto-generated release notes on every build

## Bug Fixes
- increase readiness probe initial delay to 60s
- UI-001 through UI-004 — provider pills, skill picker, version, stale session
- Enter sends message, Shift+Enter creates new line
- restore autocommit=True — required for CREATE INDEX CONCURRENTLY
- add checkpoint_migrations to migration wipe list
- prepare_threshold=None not 0 for Neon PgBouncer compatibility

## Documentation
- add UI-004 — stale session shows completion banner instead of data unavailable message
- runbook for PostgreSQL + Neon + psycopg3 + LangGraph setup issues
- add UI-003 — About in avatar menu showing version number

