# v1.0.6 — 2026-05-14

## Features
- per-user theme colour picker in avatar menu

## Bug Fixes
- avatar menu hover visible + sign out uses theme color
- theme save and load both broken — settings router never registered
- load theme inside fetchFlows — avoids 401 race condition on mount
- new chat no longer redirects to settings when some providers not configured
- theme applies to greeting chakra, user bubbles, progress dots, favicon
- theme colours now apply correctly + swatches centered
- theme swatches 26px → 18px
- usage bar background matches chat area (var(--bg), remove tint)
- purge all off-brand colours — saffron-only palette
- remove all remaining blue tints — replace with brand saffron/neutral
- no-providers banner only shows after flows API responds — prevents login flicker
- SF-007/SF-008 — completion banner dismiss + session type conversion
- health check retries up to 12x with 10s gap — handles pod switchover delay

