# v1.0.12 — 2026-05-14

## Bug Fixes
- disable Docker build cache for both images
- disable UI build cache — force clean rebuild every time
- pull latest main before Docker build — ensures image has all commits

