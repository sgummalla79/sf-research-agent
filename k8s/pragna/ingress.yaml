apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pragna
  namespace: pragna
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - pragna.sgummallaworks.com
      secretName: pragna-tls
  rules:
    - host: pragna.sgummallaworks.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: pragna-api
                port:
                  number: 8000
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: pragna-api
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: pragna-ui
                port:
                  number: 80