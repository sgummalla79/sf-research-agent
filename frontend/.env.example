# Auth0 — used by the frontend to initiate social OAuth redirects
# These are PUBLIC values — safe to commit (no secrets here)
VITE_AUTH0_DOMAIN=your-tenant.us.auth0.com
VITE_AUTH0_CLIENT_ID=your_spa_client_id
